doctype html
html(lang="en").h-100.d-flex.flex-column
  head
    title= Title
    script.
      var gon = !{JSON.stringify(gon)};
  body.d-flex.flex-column.flex-grow-1
    nav.navbar.navbar-expand-lg.navbar-light.bg-faded
      a.navbar-brand(href="/") Chat
      button.navbar-toggler(data-toggle="collapse" data-target="#navbarSupportedContent")
        span.navbar-toggler-icon
      .collapse.navbar-collapse
        ul.navbar-nav.mr-auto
          li.nav-item.active
            a.nav-link(href="/") Home
    #content.container.mt-5.mb-5.d-flex.flex-column.flex-grow-1
      #main.row.flex-grow-1
      // block content
    //- script(src='/assets/vendors.js')
    script(src='/assets/app.js')
